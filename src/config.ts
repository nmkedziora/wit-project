export const CONFIG = {
  db: {
    host: "localhost",
    user: "witapp",
    /*  ----------------------------------------
        Never ever do such things in production!
        ----------------------------------------
        Normally you would read the credentials
        from a file with restricted access.
    */
    password: "witapp",
    database: "wit",
  },
};
