// ----------------------------------------
// Never ever do such things in production!
// ----------------------------------------
// Normally you would read the credentials from a file with restricted access.

export const DB_CONFIG = {
  host: "localhost",
  user: "witapp",
  password: "witapp",
  db: "wit",
};
